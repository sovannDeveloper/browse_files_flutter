package com.kedtec.browse_files_flutter_example

import io.flutter.embedding.android.FlutterActivity

/**
 * Nothing to add: the camera, the pickers and the FileProvider they need all live in the
 * plugin, so the host app is a stock FlutterActivity.
 */
class MainActivity : FlutterActivity()
